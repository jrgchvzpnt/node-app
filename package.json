{
  "name": "node-app",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.17.1"
  },
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Ejecutando pruebas\" && exit 0"
  },
  "scripts": {
    "test": "jest --coverage"
  },
  "jest": {
    "coverageReporters": ["lcov", "text"]
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
